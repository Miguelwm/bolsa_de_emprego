<%= form_for(conta_atual.perfil.interesses_activos.find_by(interessado_id: @entidade.perfil.id),
           html: { method: :delete }) do |f| %>
<%= f.submit "Retirar interesse", class: "btn btn-danger" %>
<% end %>
