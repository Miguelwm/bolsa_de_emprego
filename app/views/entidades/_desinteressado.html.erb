<%= form_for(conta_atual.perfil.interesses_activos.find_by(interessado_em_id: @conta.id),
             html: { method: :delete }) do |f| %>
  <%= f.submit "Retirar interesse", class: "btn btn-primary" %>
<% end %>
